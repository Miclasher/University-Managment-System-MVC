@model IEnumerable<University.Models.Teacher>;

@{
	ViewData["Title"] = "Teacher list";
}

<div class="container">
    <div class="row pt-4">
        <div class="col-6 pb-3">
            <h2 class="text-start">Courses list</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Create" asp-controller="Teachers" class="btn btn-dark">Create teacher</a>
        </div>
    </div>
    <table class="table table-borderless table-striped">
        <thead>
            <tr>
                <th>
                    Teacher name
                </th>
                <th>
                    Teacher surname
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @obj.FirstName
                    </td>
                    <td>
                        @obj.LastName
                    </td>
                    <td>
                        <a asp-action="Edit" asp-controller="Teachers" asp-route-id="@obj.Id" class="btn btn-dark">Edit</a>
                    </td>
                    <td>
                        <a asp-action="Delete" asp-controller="Teachers" asp-route-id="@obj.Id" class="btn btn-outline-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
