@model IEnumerable<Group>;

@{
	ViewData["Title"] = "Group list";
	bool teachersPresent = (bool?)ViewData["TeachersPresent"] ?? false;
	bool coursesPresent = (bool?)ViewData["CoursesPresent"] ?? false;
}

<div class="container">
	<div class="row pt-4">
		<div class="col-6 pb-3">
			<h2 class="text-start">Groups list</h2>

			@if (ViewBag.ErrorMessage is not null)
			{
				<p class="text-danger">@ViewBag.ErrorMessage</p>
			}
		</div>
		<div class="col-6 text-end">
			@if (teachersPresent && coursesPresent)
			{
				<a asp-action="Create" asp-controller="Groups" class="btn btn-dark">Create Group</a>
			}
			else
			{
				<p class="text-danger">Please add teachers and courses to create group</p>
			}
		</div>
	</div>
	<table class="table table-borderless table-striped">
		<thead>
			<tr>
				<th>
					Group name
				</th>
				<th>
					Group tutor
				</th>
				<th>
					Students count
				</th>
				<th></th>
				<th>
					Actions
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var obj in Model)
			{
				<tr>
					<td>
						@obj.Name
					</td>
					<td>
						@obj.Teacher.LastName @obj.Teacher.FirstName
					</td>
					<td>
						@obj.Students.Count
					</td>
					<td>
						<a asp-action="Edit" asp-controller="Groups" asp-route-id="@obj.Id" class="btn btn-dark">Edit</a>
					</td>
					<td>
						<a asp-action="Delete" asp-controller="Groups" asp-route-id="@obj.Id" class="btn btn-outline-danger">Delete</a>
					</td>
					<td>
						<a asp-action="Clear" asp-controller="Groups" asp-route-id="@obj.Id" class="btn btn-outline-danger">Clear group</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>
