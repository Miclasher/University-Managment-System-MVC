@model University.Models.Group;

@{
	ViewData["Title"] = "Create group";
}

<h2>Create Group</h2>

<div class="container">
	<form asp-action="Create">
		<div class="form-group">
			<label asp-for="Name"></label>
			<input asp-for="Name" class="form-control" />
			<span asp-validation-for="Name" class="text-danger"></span>
		</div>
		<br />
		<div class="form-group">
			<label asp-for="Teacher"></label>
			<select asp-for="TeacherId" class="form-control" asp-items="@(new SelectList(ViewBag.Teachers, "Id", "FullName"))"></select>
			<span asp-validation-for="TeacherId" class="text-danger"></span>
		</div>
		<br />
		<div class="form-group">
			<label asp-for="Course"></label>
			<select asp-for="CourseId" class="form-control" asp-items="@(new SelectList(ViewBag.Courses, "Id", "Name"))"></select>
			<span asp-validation-for="CourseId" class="text-danger"></span>
		</div>
		<br /><br />
		<button type="submit" class="btn btn-dark">Create</button>
	</form>
</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}
